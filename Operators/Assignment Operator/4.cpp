/*
/=: This assignment operator is used to divide the left operand with the right operand and then assign it to a 
variable on the left.
*/

#include<iostream>
using namespace std;
int main(){
    int num = 10;
    num /= 2; // num = num / 2;
    cout << num;
    return 0;
}