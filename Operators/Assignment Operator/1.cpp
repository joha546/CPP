/*
+=: This assignment operator is used to add the left operand with the right operand and then assign it to a 
variable on the left.
*/

#include<iostream>
using namespace std;

int main(){
    int num = 10;
    num += 20; // num = num + 20;
    cout << num;
    return 0;
}