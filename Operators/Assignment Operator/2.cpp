/*
 -=: This assignment operator is used to subtract the left operand with the right operand and then assign it to a 
 variable on the left.
*/

#include<iostream>
using namespace std;
int main() {
    int num = 20;
    num -= 10; // num = num - 10;
    cout << num;
    return 0;
}