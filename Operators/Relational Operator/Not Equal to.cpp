#include<iostream>
using namespace std;

int main() {
    int a = 10;
    int b = 20;
    int c = 20;
    cout << (a != b) << endl;
    cout << (b != c);
    // Returns true if the left operand is not equal to the right operand
    return 0;
}